# Frontend — Zync

HTML/CSS/JS puro, sem build step.

- `assets/css/` — estilos
- `assets/js/` — scripts
- `assets/img/` — imagens
- `pages/` — páginas do app (dashboard, login, etc.) além da landing page (`index.html`, na raiz do projeto)
