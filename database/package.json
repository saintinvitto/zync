{
  "name": "zync-database-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Scripts de backup/restore do banco Postgres do Zync",
  "scripts": {
    "backup": "node scripts/backup.js",
    "restore": "node scripts/restore.js",
    "setup-staging": "node scripts/setup-staging-schema.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
